# Handoff: things only LaBroi can do

Live-verification items land here as stories complete. Check them off, then tell Claude so the story's `passes` flag can be set with notes.

## Now

- [ ] Undertone switchover (rebrand shipped 2026-09-05): Undertone.app is in /Applications with a violet icon, same permissions. When ready: quit the old Murmur.app, launch Undertone (your settings and history migrate automatically), re-enter your Groq key once (the encrypted store follows the app name), then drag Murmur.app to the Trash.
- [x] Old iOS App Store listing renamed to Undertone Voice Dictation (2026-09-07). Its bundle id and SKU are frozen by Apple and invisible to users; murmur iOS gets a fresh app record later (US-034).
- [x] The undertone repo's 5 Dependabot alerts: patched by an agent 2026-09-07, awaiting GitHub's rescan to confirm they close.
- [x] US-040 done 2026-09-08: history rewritten and rescanned clean, repo recreated fresh and flipped PUBLIC, v0.1.0 released. If any other machine has an old clone of murmur, re-clone it; the archive lives private at lbwalton/murmur-prerewrite-archive and can be deleted once you are comfortable.

## Shipped

- [ ] v0.1.7 published 2026-09-24 (brain dump notes with sorting, lead-ins, duplicates and reminders; transform; the first press after a wake). Both platforms, notarized, auto-update live; installed on the Mac. On the PC: let it update itself (murmur checks at launch), then three quick checks. (1) The home tab's what's new line reads 0.1.7. (2) US-054 on Windows: in Notepad, select a sentence, hold the transform chord, say "make this shorter", and the shorter sentence replaces it; paste-last brings the original back. (3) US-052 on Windows: set one reminder time a couple of minutes ahead and confirm the notification arrives once.

- [x] v0.1.6 published 2026-09-17: the full test-cycle release (smart lists, provider catalog and key ring, paste-last chord, license switching, diagnostics, the self-healing mic, tray and paste fixes, what's new, Eze Media LLC attribution). Both platforms, notarized, auto-update live.

## Live verification queue

The brain dump branch (US-050 to US-052 and US-055 to US-058) merged to main 2026-09-22; every story on it is now verified (US-057 and US-058 closed 2026-09-24). US-053 is main-branch work and stays open below.

- [ ] US-063 download links that never go stale (built 2026-09-26 on feature/distribution): after the next release is published with step 6 of RELEASING.md, (1) on the Mac, open a private browser window, go to https://github.com/lbwalton/murmur/releases/latest/download/murmur-mac.dmg and confirm murmur-mac.dmg downloads by itself, with no release page to read; open it and the window should show murmur and the Applications folder. (2) On the PC, open a private window in Edge, go to https://github.com/lbwalton/murmur/releases/latest/download/murmur-windows.exe and confirm murmur-windows.exe downloads. If Edge itself warns in its downloads list before you can open the file, write down its exact words so the download page can explain them. (3) Run it: the blue Windows protected your PC screen should appear; click More info, then Run anyway, and murmur should install over your current copy and open. (4) Separately, the copies of murmur you already have installed on the Mac and the PC should update to the new version on their own, as always; the home tab's what's new line shows the new number. (5) Tell Claude which steps looked as described, and anything that did not.

- [x] US-062 list lead-ins, live-verified 2026-09-24 on the decision model: spoken lists file under a plain lead line with the items nested as checkboxes, a clause comma before the list no longer confuses it, and a duplicate inside a mixed list is held while its new siblings file.

- [x] US-054 transform, live-verified on the Mac 2026-09-23 (all six steps: list replaced the note, clipboard survived, unreadable selection refused, injection not obeyed, too-long refused with the words kept, memory-only original restored by paste-last). One step left for the PC after the next published release: in Notepad, type a sentence, select it, hold your transform chord, say "make this shorter", and confirm the shorter sentence replaces it; then press your paste-last chord and the original comes back.

- [x] US-061 overlay look, live-verified 2026-09-25 (all three looks; bars, speckle, and pulse read over white in bare with the ink edge; no speech in the full pill). Steps kept for reference (built 2026-09-22 on feature/time-back; bare's ink edge added 2026-09-25 after the preview): test 1 passed on 2026-09-25 for pill, compact, and bars in bare, and speckle and pulse vanished on white, which the ink edge fixes. What is left, with Settings, dictation, Overlay look set to bare: (1) set Waveform to speckle, open a white document, and dictate a sentence: the dust should read clearly, each dot with a faint dark edge, and nothing should look cut off at the top or bottom. (2) Set Waveform to pulse and do the same: the rings should fade away on their own as they spread. (3) Set Waveform back to bars and dictate over the white document once more: the bars gained the same faint edge and should still look crisp, not smudged together. (4) With speckle on, press the hotkey and say nothing: the no speech message should come in the full pill, and the next dictation should show the dust at normal size again. (5) Dictate once over a dark app to confirm all of it still looks as it did.

- [ ] US-060 time back arrives (built 2026-09-22 on feature/time-back): on a real working day, keep dictating until the home card first reads 30 min. On that same take, expect exactly one notification titled time back reading "30 minutes earned back today.", and none again for 30 that day even if a slow take pulls the number under and back over; the next one arrives at 1 hour, reading "1 hour earned back today." Clicking it opens the wrap-up. With the recap on, the notification at your set time should end with your day's number, for example "... words today. 47 minutes back." Hover the tray icon: the tooltip should read "murmur · 47 min back today" (plain murmur on a day with nothing yet), and it should update after each take. Finally flip Settings, recap, Milestone alerts to Off and cross the next mark: nothing should appear, and logs/murmur.log should carry a "[timeback] milestone mark=... outcome=off" line for the take.

- [x] US-059 time back, live-verified 2026-09-25 (card, wrap-up, analytics, typing speed restatement, tray tooltip). Steps kept for reference (built 2026-09-22 on feature/time-back): dictate once, then open the home tab. Above the log a card labeled back today should show a number like 3 min with this month and lifetime beneath it, and the wrap-up tab should show the same number under time back with the sentence "At your typing speed of 40 wpm, that is roughly ... you did not spend typing." Then go to Settings, recap, Typing speed, set it to 80, and come back: the home card and the wrap-up should both read a smaller number at once (half the typing time, same speaking time), and the analytics tab's this month and lifetime rows should carry a time back stat that moved with it. Set it back to 40, or to whatever you really type at.

- [x] US-055 duplicates and completions, live-verified 2026-09-22 (duplicate held and filed anyway once; completion held as a note and ticked, backup and reminder count confirmed; hand-edit refusal; comma-run case holding only its own piece). Steps kept for reference (built 2026-09-22 on feature/brain-dump; works on the chat connection, no TypeSafe key needed): with sorting on, first make sure the item you will report done is on your list (say "Email Bob the deck." and let it file), then dump an errand you filed earlier in nearly the same words plus a line that reports the deck done, for example "Renew the domain. I finished the deck for Bob." Expect todo.md unchanged, and under Last note in settings two held lines: the first reading "looks like a duplicate of:" with your earlier line quoted, the second "sounds like you finished:" with the deck item quoted (a completion is a statement, so it is labeled a note and offers Tick it and Skip only). Press File it anyway on the first: it lands once with its link. Press Tick it on the second: in Obsidian the deck item's box is ticked and nothing else in the file moved, a copy of the file as it was sits in Application Support/murmur/notes-backups, and the next Test reminder counts one fewer open task. Then dump another completion, and before pressing Tick it change or delete any line in todo.md by hand; Tick it should refuse and say the file changed, with the file left exactly as you edited it. Last, say a two-item sentence where one item is already on your list, for example "I need to order toner, and renew the domain" when the domain is already there: expect the toner filed on its own line, the domain half held with your earlier line quoted, and the log carrying compared pieces=2 of=1 held=1.

- [x] US-058 held lines, verified by smoke 2026-09-24 with LaBroi's approval: on TypeSafe jev every real line scored 0.95 or higher, even deliberately vague ones, so no live line could be held as unsure; the hold and Sort again paths are smoke-proven, and the new [sort] confidence log line shows the numbers for any future check.

- [x] US-057 decision-model sorting, live-verified 2026-09-23/24 on LaBroi's TypeSafe key (real dumps filed, cut, and held duplicates like the chat sorter). The no-key fallback step was skipped by agreement; it is smoke-checked.

- [x] US-056 sentence splitting, live-verified 2026-09-22 (two-item sentence cut with split=1, non-list sentence kept whole with split=0). Steps kept for reference (built 2026-09-21 on feature/brain-dump; partly seen live 2026-09-22: a three-item run landed as three checkboxes, but with split=0, because cleanup had already made it a list before the sort): with sorting on, hold the note chord and say a two-item sentence joined by "and", which cleanup leaves as one sentence, for example "I need to renew the domain, and pay the water bill." Expect two checkboxes in todo.md, each a piece of that sentence with every word kept and only the joining words gone, each linking to the note, and the log's filed line carrying split=1. Then say something with a joining word that is NOT a list, for example "I want to talk about rock and roll with Sam," and expect it to file as one line with split=0.

- [x] US-052 task reminders, live-verified on the Mac 2026-09-21 (Test read 7 open tasks; one scheduled reminder fired once at the set time). After the next published release: on the PC, set one reminder time and confirm the readout arrives there too. on the Mac, under Settings, notes, press Test beside Task reminders. With open tasks in your list it should show a notification reading like "3 open tasks: ..." and clicking it should open todo.md; with everything ticked it should say nothing is open; with no tasks file it should say so. Then set reminders to On, clear two of the three times so only one remains, and set that one a couple of minutes ahead: the notification should arrive once, at that time, and not repeat. The real target is the PC: do the same there, since that machine has no briefing rig. If notifications never appear, the Test button now says whether the system is blocking them.

- [x] US-051 brain dump sorting, live-verified green 2026-09-20 (five dumps filed with topic headings and links; the one-line-per-sentence limit it exposed is specced as US-056): under Settings, notes, set Sort into tasks and ideas to On and leave Sort connection on Same as cleanup. Hold the note chord and dump a mixed ramble, for example: "I need to call the dentist about Thursday. Maybe the wizard could ask for the vault path on first run. The standup felt long today. Email Bob the deck." Expect the pill to say noted as before, then within a few seconds murmur/todo.md in your vault holds a '## <today>' heading with two checkbox lines under it (the dentist and the deck) and murmur/ideas.md holds the wizard line under the same heading, each line ending in a link like ([10:32](inbox/2026-09-20.md)) that opens the day's inbox note when clicked in Obsidian; a second dump the same day should join the heading already there rather than start a new one; the inbox entry itself is unchanged and the standup sentence stays only there. The Last note line in settings should read "filed 2 tasks and 1 idea". Then: (1) note that Sort again is absent after a successful filing (it appears only after a rejected or failed sort, so a note can never be filed twice). (2) Set Sort connection to Separate provider, pick a provider you hold a key for, save the key, Test, dump another ramble, and confirm it files; the log line ends in slot=separate. (3) Turn sorting Off, dump a note, and confirm only the inbox changes. (4) Turn sorting back on and set Name each note to On, then dump two unrelated rambles a minute apart: each should get its own heading naming what it was about (at most five words), the same name over that note's tasks and its ideas, and your sentences unchanged under them. Anything that felt off goes in the notes.

- [x] US-050 brain dump capture, live-verified 2026-09-20 (steps 1 to 4 in the log; the renamed-vault case is now smoke-covered and the fixed build is installed): under Settings, notes, capture a note chord (Ctrl+F11 is a safe pick), click Choose folder, and pick your Obsidian vault. From another app, hold the chord and speak. Expect the pill to show a gold edge with a small note tag and say noted, and within seconds the words sit in murmur/inbox/<today>.md inside the vault under a ## time heading, formatted like a dictation, with Obsidian showing the file whether or not it was open. Then three more: (1) open File layout, set the path to Daily/{date}.md (or your daily note's real path), check the Today preview, dictate a note, and confirm it appended there; Reset to default afterwards. (2) Clear the folder, press the chord: the pill should say set a notes folder in settings with the soft cue, and nothing records. (3) Rename the vault folder for a minute, dictate a note, and confirm Last note says saved to murmur's data folder, the note is in Application Support/murmur/notes/murmur/inbox/, and logs/murmur.log has a notes folder unreachable line; rename it back. The tray menu should show Open today's inbox whenever a folder is set.

- [x] US-053 first press after wake, live-verified 2026-09-24: after a real wake the first press delivered (one silent re-arm line, then a healthy delivery). The press-time rescue for a mic that stays dead is smoke-proven; if a first press ever fails again, note the time and tell Claude.
- [x] US-046 what's new (built 2026-09-16, in the next installed build): the home tab should show a "what's new in 0.1.6" line with an amber dot; open it, read the release story, and the dot should stay gone across restarts. The full-loop check comes free with the next real update after v0.1.6.
- [x] US-048 silent-stream heal (built 2026-09-16, in the next installed build): with murmur running, quit and reopen Wave Link, then dictate. Expect one soft no-speech flash (the heal fires here) and the very next dictation delivering normally, no murmur relaunch. The log shows the pair: a nospeech with peak near 0.000 plus the re-arming line, then a delivered line with a healthy peak.
- [x] US-047 diagnostics (built 2026-09-15, in the next installed build): tap the mute ring on the Wave:3 (or unplug the mic), dictate, and check logs/murmur.log shows a nospeech line with a near-zero peak; then Save report from Settings, setup, Diagnostics and read the file: version, hook state, settings summary with counts only, log tail, and none of your words anywhere.
- [ ] Copyright assignment paperwork: the app, README, and package.json now credit Eze Media LLC. Make it accurate on paper: sign the one-page assignment drafted at ~/Documents/murmur-copyright-assignment-2026.md (you to your LLC), date it, keep it with your LLC records. Not legal advice; have a lawyer glance at it if you want certainty.
- [x] US-041 license switching (built 2026-09-14, needs the next build): activate your founder key, then Deactivate on the setup tab. The app should show the free experience with a Reactivate Pro button; Reactivate must restore Pro without re-entering the key. Forget key only when you actually mean to delete it.
- [x] US-045 paste-last chord (built 2026-09-14, needs the next shipped build): capture a chord under Settings, dictation, Paste last dictation (Ctrl+F12 is a safe pick; Ctrl+Alt+V will be refused because it contains your dictation hotkey). Then: dictate something, click into another app, press the chord, the text pastes again. Hold the chord: exactly one paste. Press it mid-recording: nothing.
- [ ] US-044 provider connections: steps 2 and 3 live-verified 2026-09-17; only step 1 remains: (1) the pricing-watch workflow runs on your Claude subscription: in a terminal run `claude setup-token`, then add the printed token to the lbwalton/murmur repo as the CLAUDE_CODE_OAUTH_TOKEN secret (Settings, Secrets and variables, Actions). Token lives one year and its usage counts against your subscription, not a separate API bill (verified 2026-09-13). Without it the Monday check just fails quietly. (2) The key ring (needs the next build): your Groq key should appear as a labeled line item (Groq, gsk_ mask) under the key field, auto-assigned by its prefix. Switch to OpenAI: the field should say no OpenAI key yet and glow; paste an sk- key and BOTH keys should list, each removable. Switch back and forth: dictation uses the right key each time with no re-entering. (3) Set Cleanup connection to Separate, pick a second provider you hold a key for, save its key, Test, and dictate: transcription on Groq, cleanup on the second provider. Flip Price refresh off and on while you are there.
- [x] US-043 smart lists (built 2026-09-13, needs the next shipped build): turn on Settings, dictation, Smart lists. (1) Dictate "I need to pack socks, shirts, shoes, pants": expect a bulleted list. (2) Dictate "first email the team, then update the deck, then send it to the client": expect a numbered list. (3) Say "bullet point socks bullet point shoes": expect dash lines even if the cleanup model is off. Check one plain text field and one markdown app. With the toggle off, dictation must behave exactly as before.
- [x] US-005 tray window raise (fixed 2026-09-13, needs the next shipped build): with another app active and its windows covering screen center, click Open murmur in the tray. The settings window must come to the front immediately. Before the fix it opened buried behind the active app, so the click looked dead.
- [x] US-011 paste settle (fixed 2026-09-13, needs the next shipped build): dictate into a busy app and confirm the text pastes; whatever the clipboard held before should come back about a second and a half later (copy something first, dictate, then paste again manually to see the original return). Bonus check: two quick dictations back to back both land intact.

- [x] US-008 sleep recovery (v0.1.5): shipped notarized to both platforms, and LaBroi live-verified 2026-09-12 that the hotkey captures audio again after real sleep and wake on both the Mac and the PC. Story complete.

- [ ] US-041 pro unlock, what remains: (1) DONE 2026-09-14: Pro conveniences decided (profiles now, provider connections shipped free) and your founder key was issued directly (order id founder-owner-001) and activated. (2) Still open: one real test purchase at the live Payment Link (code FOUNDERS takes it to 39 dollars) to prove the paid path end to end, then issue that buyer key with scripts/issue-license.js. (3) Try profiles for real: save your Groq setup under a name, change the base URL, apply the profile to snap back (US-042). (4) Decide when the FOUNDERS code retires.





- [x] US-024 packaged mac app: effectively verified by daily use (you have run the installed, notarized app with permissions, key, and dictation since v0.1.x). Confirm and this closes.
- [x] US-025 windows: effectively verified by the PC auto-updating to v0.1.5 and the US-008 live check passing there 2026-09-12. Confirm the original install used the exe and autostart survives reboot, and this closes.

- [x] US-020 recap, two quick steps in the installed app: (1) click the recap Test button once more now that notifications are allowed; a murmur recap banner with today's numbers should appear instantly. (2) For the full loop: turn Daily recap on, set a time two minutes out, wait for the banner, click it, confirm you land on the wrap-up tab.

## Known follow-ups (not blocking)

- [ ] Brain dump recall wiring (pairs with US-050/051 when they ship): add a section to the 7am briefing and 6pm recap scripts that reads murmur/todo.md and the day's murmur/inbox note from the vault: open tasks first, captured ideas after.


- [ ] Synthetic smoke can flake on a real Mac (seen 2026-09-10 evening): the recording, recordingRecovery, recordingRevive, and dictationLoop checks all failed locally while pristine HEAD and CI stayed green. Cause is smoke-only: the muted oscillator feeding the hidden audio window stops being rendered by Chromium after ~2 quantums when the machine's audio state shifts (e.g. display asleep), so the watchdog reads it as a real stall. A live mic does not hit this (the input device clocks the graph). If it starts flaking in CI, make the synthetic source self-clocked instead of destination-pulled (e.g. drive the worklet without depending on ctx.destination consumption). Product capture code is unaffected.

- [ ] Hotkeys went quiet once (2026-09-23, about 08:46 to 08:49 UTC): murmur was running and not frozen, but no chord reached it until a restart. Cause unknown. If it happens again, before restarting, note which app was in front and the last keys you pressed, and tell Claude; logs/murmur.log will be read against that.

## Standing needs

- A real Groq API key on this Mac for live transcription tests (never committed, entered through the app).
- Your Windows machine for US-025 verification when packaging lands.
